export type Role = "admin" | "doctor" | "hospital" | "receptionist";

export const ROLES = {
  ADMIN: "admin" as Role,
  DOCTOR: "doctor" as Role,
  HOSPITAL: "hospital" as Role,
  RECEPTIONIST: "receptionist" as Role,
};
